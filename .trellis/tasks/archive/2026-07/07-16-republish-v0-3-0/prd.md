# 补发 GitHub Release v0.3.0

## 目标

补齐 v0.3.0 的远端标签推送，使已有发布提交真正触发 GitHub Release workflow。

## 已确认事实

- 本地发布提交为 `67eaf42`，本地标签 `v0.3.0` 指向该提交。
- `origin/main` 已指向 `9f66e9d`，包含发布提交。
- 远端此前没有 `v0.3.0` 标签、Release 或对应 workflow run。
- 根因是之前的复合推送没有让标签到达远端，且未做远端确认。

## 要求与验收

- 将本地 `v0.3.0` 标签推送到 `origin`。
- 远端标签必须指向 `67eaf42`。
- GitHub Release workflow 必须被标签 push 触发，run 状态可见。
- 不重复修改发布文件、不重复创建提交、不监控后续构建。

## 不在范围内

- 不修改应用代码和发布文案。
- 不删除或移动现有标签。
- 不等待 workflow 完成或核验最终安装包。
